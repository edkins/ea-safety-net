{
	"type": "object",
	"properties": {
		"slackTokenHash": {
			"type": "string",
			"pattern": "^[0-9a-f]{64}$"
		},
		"oauthToken": {
			"type": "string"
		}
	},
	"required": ["slackTokenHash","oauthToken"],
	"additionalProperties": false
}

